{
  "_comment": "OPTIONAL backstop. A hook runs a shell command and CANNOT call the Slack MCP, so this posts via a Slack incoming webhook (SLACK_WEBHOOK_URL) from a small script. Use only if you want a notification to fire even when the chain is interrupted; otherwise the slack-notifier agent (MCP) is sufficient. Add this under the top-level \"hooks\" key in .claude/settings.json.",
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "security-reviewer",
        "hooks": [
          { "type": "command", "command": "$CLAUDE_PROJECT_DIR/.claude/scripts/notify-slack.sh 'ship: review stage complete'" }
        ]
      }
    ]
  }
}
